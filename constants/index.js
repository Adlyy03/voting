
import { logo, cards, path, logoSmall, empty } from './images';
import { icons } from './icons'; // Meskipun kita belum membuatnya, ada baiknya untuk menambahkannya sekarang

export const images = { logo, cards, path, logoSmall, empty };
export const icons = icons;
